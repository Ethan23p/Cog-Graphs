node_modules/

# secrets — CLAUDE_CODE_OAUTH_TOKEN lives here (Bun auto-loads it); never commit
.env

# eval run outputs (transcripts, summaries, stats) — local history only
testing/artifacts/

# git worktrees — committing one leaves a gitlink with no .gitmodules entry,
# which breaks any checkout that runs `submodule update`
.claude/worktrees/
