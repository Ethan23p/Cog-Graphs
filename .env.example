# Copy to .env (gitignored). Bun auto-loads it; the harness passes it through to
# the in-loop session. Generate the token with:  claude setup-token
#
# ANTHROPIC_API_KEY is deliberately stripped by the runtime to keep a single,
# unambiguous auth path — setting it here will not help.
CLAUDE_CODE_OAUTH_TOKEN=
